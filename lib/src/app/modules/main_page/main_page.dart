import 'package:flutter/material.dart';
import '../forms_page/FormsPage.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: FormsPage(),
    );
  }
}
